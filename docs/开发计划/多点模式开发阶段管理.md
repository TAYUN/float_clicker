# 多点模式开发阶段管理

## 1. 文档目的

多点模式包含基础多点、通用调度内核、后期多配置执行和高级连招等多个阶段。为了避免开发过程中范围失控，需要用本文档记录：

- 每个阶段的目标。
- 已完成任务。
- 未完成任务。
- 阶段边界和暂不做内容。
- 设计调整记录。
- 当前推荐下一步。

本文档是开发过程中的活文档。随着真机验证、代码实现和需求讨论推进，阶段计划可以调整，但每次调整都应记录原因和影响范围。

## 2. 状态标记

任务状态统一使用以下标记：

```text
[未开始] 尚未进入实现
[进行中] 正在实现或联调
[已完成] 已实现并通过基本验证
[已验证] 已完成真机或回归验收
[阻塞] 需要先解决外部问题或设计问题
[延期] 明确不进入当前阶段
[调整] 原设计发生变化，需要看调整记录
```

使用规则：

- 一个任务完成代码实现后标记为 `[已完成]`。
- 通过对应验收或回归后标记为 `[已验证]`。
- 如果需求边界变化，先标记 `[调整]`，并在“设计调整记录”中记录原因。
- 不确定是否进入当前阶段的内容先标记 `[延期]`，避免默认混入当前开发。
- 只有计划文档或计划草案时仍保持 `[未开始]`；`[进行中]` 只用于已经开始代码实现或联调的阶段。
- 未实现功能只能写入计划、待实现或暂不做内容，不能写入验收记录或真机测试项。
- 真机测试模板只在对应功能实现完成后发出；专项验收只能覆盖当前代码中已经存在的能力。

## 3. 迭代原则

- 开始开发前尽量把交互、状态、异常和持久化设计考虑完整，减少后续返工。
- 设计不是一次性定稿。开发中如果发现更稳、更简单或更符合用户使用习惯的方案，可以调整文档和计划。
- 所有调整都应先明确影响范围：Flutter 页面、持久化、MethodChannel、Android Overlay、调度器、验收标准。
- 除非有明确收益，不在同一阶段同时做大范围重构和新功能。
- 已稳定的单点模式必须保护，不因多点开发发生行为回退。
- 后期能力必须先进入设计文档或本计划，确认阶段边界后再实现。

## 4. 总体阶段

| 阶段 | 名称 | 状态 | 目标 |
| --- | --- | --- | --- |
| P0 | 文档与边界确认 | [已完成] | 明确基础多点第一版和后期能力边界 |
| P1 | Flutter 多点模型与持久化 | [已完成] | 建立多点数据模型、JSON 持久化和测试 |
| P2 | Flutter 多点页面 | [已完成] | 提供点位列表编辑、设置和基础入口 |
| P3 | Android 多点 Overlay | [已完成] | 分阶段显示多个点位并复用悬浮控制组件 |
| P4 | 通用手势执行与调度 | [已完成] | 支持基础多点顺序点击、暂停、继续、结束 |
| P5 | 多点联调与真机验收 | [已验证] | 完成权限、异常、横竖屏和回归验证 |
| P6 | 多配置管理 | [已验证] | 支持保存和管理多套多点配置 |
| P7 | 多配置悬浮执行区 | [已验证] | 支持加载多个配置并显示多个执行控件 |
| P8 | 高级连招能力 | [延期] | 支持长按、滑动、延迟和动作编排 |

## 5. P0：文档与边界确认

目标：

- 明确基础多点第一版只做多个固定点位按顺序点击。
- 明确高级连招和多配置执行属于后期能力。
- 明确运行、暂停、编辑状态下的操作规则。
- 明确风险控制方案。

任务：

- [已完成] 阅读单点模式、悬浮交互、全局外观、验收和横竖屏调试文档。
- [已完成] 补充基础多点状态操作矩阵。
- [已完成] 补充点位坐标、默认点位、最大点位数、禁用点位显示规则。
- [已完成] 补充暂停中编辑后的继续语义。
- [已完成] 补充 MethodChannel 错误码建议。
- [已完成] 补充首页全局 active mode 状态要求。
- [已完成] 新增后期多配置执行设计文档。
- [已完成] 新增阶段管理文档并维护状态标记。
- [已完成] 开始 P1 前再次检查 `git status`，确认不覆盖已有改动。

验收：

- [已完成] 文档能指导 P1 开发，不需要再临时决定核心语义。

## 6. P1：Flutter 多点模型与持久化

目标：

- 新增基础多点数据模型。
- 新增基础多点持久化 store。
- 为后续页面和 Android 协议提供稳定数据结构。

计划任务：

- [已完成] 新增 `MultiPointTarget`。
- [已完成] 新增 `MultiPointSettings`。
- [已完成] 新增 `MultiPointOverlayUiSettings`。
- [已完成] 新增 `MultiPointSettingsStore`。
- [已完成] 使用 `multi_point.targets_json` 保存完整点位列表。
- [已完成] 实现默认 2 个启用点位。
- [已完成] 实现最多 12 个点位约束。
- [已完成] 实现不允许删除到 0 个点位对象。
- [已完成] 实现允许禁用全部点位，但执行前校验失败。
- [已完成] 单元测试覆盖默认值、增删点、排序、禁用、坏 JSON 兜底和持久化。

暂不做：

- [延期] 多配置 profile。
- [延期] 多个悬浮执行控件。
- [延期] 长按、滑动、延迟动作。

## 7. P2：Flutter 多点页面

目标：

- 首页开放多点入口。
- 新增多点模式页面和设置页面。
- 支持基础点位列表编辑。

计划任务：

- [已完成] 首页多点入口从“开发中”调整为可进入。
- [延期] 首页运行状态升级为全局 active mode 展示，待 Android 多点快照和模式互斥协议接入后统一处理。
- [已完成] 新增多点模式页面。
- [已完成] 新增多点设置页面。
- [已完成] 支持新增、删除、启用、禁用、排序点位。
- [已完成] 运行中禁用结构编辑入口。
- [延期] 暂停中允许编辑并标记点位结构变化，待原生侧三态任务接入后完成。
- [已完成] 点位列表展示完整顺序，悬浮层后续只显示启用点位。
- [已完成] 设置页支持点击间隔、循环次数、无限循环、点击持续时间、悬浮交互模式。
- [调整] 页面错误提示本阶段覆盖无启用点位和 Android 未接入提示；权限不足、模式冲突待 MethodChannel 接入后完成。

暂不做：

- [延期] 配置搜索、分组、导入导出。
- [延期] 执行模式隐藏点位和多执行控件。

## 8. P3：Android 多点 Overlay

目标：

- 新增多点点位悬浮组件。
- 新增多点 Overlay 管理器。
- 复用单点已稳定悬浮控制组件和安全窗口逻辑。

阶段边界：

- P3 只做 Android 多点悬浮层显示、更新和位置回传。
- P3 不实现真实多点点击执行，不接入 `dispatchGesture()` 调度。
- P3 不实现暂停、继续、轮次进度和动作序列。
- P3 不做多配置执行控件，不做高级连招动作。

### 8.1 P3.1：Android 多点协议与空壳接入

目标：

- 先打通 Flutter 与 Android 的多点 Overlay 方法名、参数解析、快照返回和错误边界。
- 在不创建复杂悬浮 UI 的情况下，确认协议可被 Flutter 页面调用和后续扩展。

计划任务：

- [已完成] 接入 `showMultiPointOverlay`。
- [已完成] 接入 `hideMultiPointOverlay`。
- [已完成] 接入 `updateMultiPointTargets`。
- [已完成] 接入 `updateMultiPointOverlayUiSettings`。
- [已完成] 接入 `getMultiPointOverlaySnapshot`。
- [已完成] 定义 Android 侧多点目标数据结构。
- [已完成] 解析 Flutter 传入的点位、悬浮交互模式和外观配置。
- [已完成] Android 未支持执行时，`start/pause/resume/endMultiPointClicking` 明确返回 `unimplemented_method` 或稳定错误。

验收：

- [已完成] Flutter 可以调用多点 Overlay 相关 MethodChannel 方法，不再只停留在页面本地提示。
- [已完成] 快照能返回 `modeEnabled`、`taskRunState`、完整点位列表和悬浮 UI 配置。
- [已完成] 不影响单点模式已有 MethodChannel 和悬浮窗行为。

### 8.2 P3.2：多点点位悬浮组件

目标：

- 新增单个多点点位悬浮组件，为后续 `MultiPointOverlayManager` 管理多个点位做准备。

计划任务：

- [已完成] 新增 `MultiPointTargetOverlayComponent`。
- [已完成] 显示点位编号。
- [已完成] 支持启用点位的尺寸缩放，复用 `targetPointScale`。
- [已完成] 支持拖动点位。
- [已完成] 拖动结束后回传点位 `id`、左上角 `x/y`。
- [已完成] 复用 `OverlayWindowHelper` 的安全添加、更新、移除窗口逻辑。

验收：

- [已完成] Android 侧可以创建、移动和移除单个编号点位组件。
- [已完成] 点位拖动后坐标语义仍是悬浮组件左上角。

### 8.3 P3.3：多点 Overlay Manager 基础版

目标：

- 新增 `MultiPointOverlayManager`，管理多个启用点位的显示、隐藏和刷新。

计划任务：

- [已完成] 新增 `MultiPointOverlayManager`。
- [已完成] 悬浮层只显示启用点位。
- [已完成] 启用点位按执行顺序显示连续编号。
- [已完成] 点位新增、删除、启禁、排序后刷新悬浮点。
- [已完成] 多点 Overlay 关闭时完整移除所有多点窗口。

### 8.4 P3.4：复用悬浮交互组件

目标：

- 在多点 Overlay 中复用单点已稳定的控制条、收起图标和独立执行控件。

计划任务：

- [已完成] 复用 `ToolbarOverlayComponent`。
- [已完成] 复用 `CollapsedToolbarComponent`。
- [已完成] 复用 `ActionButtonOverlayComponent`。
- [已完成] 支持普通、简洁、极简三种悬浮交互模式。
- [已完成] 控制按钮第一版只承接状态和提示，不执行真实多点任务。

### 8.5 P3.5：位置回传与 Flutter 持久化

目标：

- 多点悬浮点和控制组件拖动后，回传 Flutter 并保存到 P1 持久化结构。

计划任务：

- [已完成] 点位拖动后回传 `onMultiPointTargetPositionChanged`。
- [已完成] Flutter 收到点位位置后更新 `multi_point.targets_json`。
- [已完成] 控制条、收起图标、独立控件位置变化后通过多点 Overlay 快照回传并保存。
- [已完成] `idle` 中点位结构变化时刷新悬浮点。

### 8.6 P3.6：边界、互斥和异常收尾

目标：

- 补齐多点 Overlay 与单点 Overlay、权限和窗口异常的边界处理。

计划任务：

- [已完成] 多点开启时如果单点已开启，返回 `mode_conflict`。
- [已完成] 悬浮窗权限缺失时返回 `overlay_permission_denied`。
- [已完成] 窗口创建或更新失败时返回 `overlay_window_unavailable`。
- [已完成] 横竖屏切换后所有多点组件保持可见。
- [已完成] 关闭多点模式时安全移除所有窗口。

风险：

- [已完成] 多点和单点互斥，避免两个 Overlay Manager 同时显示。
- [已完成] 多点组件复用单点控制条时，不能破坏单点现有按钮语义和状态回调。
- [已完成] P3 不接入真实点击执行，避免和 P4 调度器职责混在一起。

## 9. P4：通用手势执行与调度

目标：

- 支持基础多点按启用点位顺序点击。
- 支持暂停、继续、结束。
- 为后续高级连招预留动作模型。

计划任务：

### 9.1 P4.1：通用动作和手势执行器

任务：

- [已完成] 定义 `AutomationAction`，第一版只包含 tap 动作。
- [已完成] 新增或抽出 `AccessibilityGestureExecutor`。
- [已完成] 统一处理 `dispatchGesture()` 返回 false、完成、取消和无障碍服务断开。
- [已完成] 保持单点模式现有行为不回退。

### 9.2 P4.2：多点任务调度器基础版

任务：

- [已完成] 新增 `MultiPointClickScheduler` 或通用 `AutomationTaskScheduler`。
- [已完成] 按启用点位顺序生成 tap 动作。
- [已完成] 支持有限轮次和无限循环。
- [已完成] 实现 `completedRounds`、`currentRound`、`executedActionCountInCurrentRound`、`currentTargetId`。

### 9.3 P4.3：多点三态控制接入

任务：

- [已完成] 接入 `startMultiPointClicking`。
- [已完成] 接入 `pauseMultiPointClicking`。
- [已完成] 接入 `resumeMultiPointClicking`。
- [已完成] 接入 `endMultiPointClicking`。
- [已完成] 悬浮控制条和独立执行控件驱动多点任务状态。

### 9.4 P4.4：暂停编辑和边界语义

任务：

- [已完成] 实现暂停中点位结构变化后从当前轮第一个启用点位继续。
- [已完成] 实现暂停中 `repeatCount` 小于等于已完成轮次时继续即结束。
- [已完成] 执行中禁止结构编辑，暂停中允许编辑并明确刷新策略。

暂不做：

- [延期] 多任务并行执行。
- [延期] 动作级长按、滑动、延迟 UI。

## 10. P5：多点联调与真机验收

目标：

- Flutter、Android、权限、悬浮层和调度器完整打通。
- 完成基础多点第一版真机验收。

计划任务：

- [已验证] 接入 `showMultiPointOverlay`，PLQ110 真机可开启多点悬浮层并显示目标点和控制条。
- [已验证] 接入 `hideMultiPointOverlay`，关闭多点悬浮层后窗口移除并回到未启动状态。
- [已完成] 接入 `updateMultiPointSettings`，代码层已接入，真机设置项变更待补充专项验证。
- [已验证] 接入 `updateMultiPointTargets`，暂停中切换点位启用状态后可保存并用于后续继续语义。
- [已完成] 接入 `updateMultiPointOverlayUiSettings`，代码层已接入，普通/简洁/极简模式切换待补充专项验证。
- [已验证] 接入 `start/pause/resume/endMultiPointClicking`，悬浮控件和页面按钮可驱动执行、暂停、继续和结束。
- [已验证] 接入 `getMultiPointOverlaySnapshot`，页面状态能跟随原生多点快照切换为编辑中、正在点击、已暂停和未启动。
- [已验证] 处理 `mode_conflict` 的真机互斥验收。
- [已验证] 处理 `no_enabled_targets` 的真机边界验收。
- [已验证] 处理权限撤销、服务断开、手势失败；已完成悬浮窗权限撤销专项、无障碍连接态同步复验和运行中断开无障碍服务专项。
- [已验证] 横竖屏切换后所有点位和控制组件保持可见，旋转后坐标会按可见区域校正并回传持久化。
- [已验证] 回归单点模式真机验收清单，PLQ110 / Android 16 完成 `T01-T20` 全量通过。

回归命令：

```powershell
flutter analyze
flutter test
flutter build apk --debug
```

验收记录：

- [已验证] 2026-05-09，`flutter analyze` 通过。
- [已验证] 2026-05-09，`flutter test` 通过，37 个测试通过。
- [已验证] 2026-05-09，`flutter build apk --debug` 通过。
- [已验证] 2026-05-09，PLQ110 / Android 16 API 36 真机安装并启动 debug APK；`flutter install --debug` 超时后使用 `adb install -r` 安装成功。
- [已验证] 2026-05-09，首页权限卡片显示无障碍权限已连接、悬浮窗权限已开启；`dumpsys accessibility` 同步显示 `float_clicker` 服务处于 Bound services。
- [已验证] 2026-05-09，多点悬浮层显示、执行、暂停、暂停中编辑启用点位、继续、结束和关闭模式完成一轮真机闭环。
- [已验证] 2026-05-09，横竖屏切换后多点控制条和目标点仍保持在可见区域，回到竖屏后状态可继续操作。
- [已验证] 2026-05-09，单点模式最小回归通过：单点悬浮层显示、任务执行、任务结束和关闭后回到未启动。
- [已验证] 2026-05-09，`mode_conflict` 真机通过：单点开启时拒绝开启多点，多点开启时拒绝开启单点，页面状态与提示语一致。
- [已验证] 2026-05-09，`no_enabled_targets` 真机通过：全部点位禁用时允许保持多点编辑，执行前会拒绝并提示“请至少启用 1 个点位后再执行。”。
- [已验证] 2026-05-09，悬浮窗权限撤销专项通过：撤销后页面回到未启动，再次开启时提示权限未开启，恢复权限后可重新开启。
- [已验证] 2026-05-09，修复并复验无障碍连接态同步：统一 Android 侧 `accessibilityGranted/accessibilityConnected` 状态来源，补强无障碍服务创建、连接、解绑和销毁时的实例维护；首页真机显示无障碍“已连接”、悬浮窗“已开启”，`dumpsys accessibility` 同步显示服务处于 Bound services；单点页和多点页可正常进入。自动化回归通过 `flutter analyze`、`flutter test`、`flutter build apk --debug`。
- [已验证] 2026-05-09，运行中断开无障碍服务专项通过：多点和单点任务运行时，通过右下角系统无障碍快捷图标关闭 Float Clicker 服务后，`dumpsys accessibility` 显示 `Enabled services:{}`、`Bound services:{}`；返回 App 等待 5 秒内页面和悬浮控制条均回到非执行态。无障碍关闭时再次执行会提示服务未连接；重新开启后单点和多点均可恢复执行。最近 300 行 logcat 未发现应用侧 `FATAL EXCEPTION`、`E/flutter` 或 `dispatchGesture` 异常。
- [已验证] 2026-05-09，完整单点模式真机验收清单通过：PLQ110 / Android 16 / debug 包完成 `T01-T20`，覆盖首页权限基线、单点三态、普通/简洁/极简悬浮交互、全局外观缩放、横竖屏、后台保活、无障碍关闭执行拒绝、悬浮窗权限撤销与恢复、权限恢复后的旧位置和旧模式恢复；未见崩溃，未见 `E/flutter`。

风险与未完成项：

- [已验证] “运行中断开无障碍服务”专项复验已完成；多点和单点任务都会停止，页面状态、悬浮控制条和系统 `Bound services` 状态一致。
- [已验证] 旋转验收中观察到点位坐标会被可见区域校正并持久化，属于既定横竖屏边界策略；多点横竖屏专项和单点 `T15` 均已确认组件保持可见、可拖动、可继续执行。
- [已验证] 单点完整真机验收清单已全量补跑并通过；如继续多机并行验收，只需按 `T01-T20` 模板补充机型差异。

## 11. P6：多配置管理

目标：

- 支持保存和管理多套多点配置。
- 将基础多点当前配置升级为 profile。

计划任务：

- [已完成] 新增 `MultiPointProfile`，复用现有点位、点击参数和多点悬浮编辑设置。
- [已完成] 新增 `multi_point.profiles_json`。
- [已完成] 新增 `multi_point.active_profile_id`。
- [已完成] 迁移旧单配置为默认 profile，并保留旧散 key 同步写入作为兼容来源。
- [已完成] 支持新建、复制、重命名、删除配置。
- [已完成] 支持选择某个配置进入编辑。
- [已完成] 运行中禁止配置管理；暂停和 idle 可切换配置并同步当前多点 Overlay。

验收记录：

- [已完成] 2026-05-09，`flutter analyze` 通过。
- [已完成] 2026-05-09，`flutter test` 通过，47 个测试通过。
- [已完成] 2026-05-09，`flutter build apk --debug` 通过。
- [已验证] 2026-05-09，P6 分支合入基础多点执行 hotfix 后，真机验证多点执行可以真实点击到底层应用。
- [已验证] 2026-05-09，P6 配置管理真机验收通过：新建、复制、重命名、删除、切换配置均可正常操作。
- [已验证] 2026-05-09，配置操作 -> 重命名专项复验通过，未再出现 Flutter 构建作用域断言。
- [已验证] 2026-05-09，单点最小回归已完成，旧能力未发现回退。

风险与未完成项：

- [已验证] P6 profile 管理、基础多点真实点击和单点最小回归已完成真机验收。
- [延期] 多配置悬浮执行区、多个执行控件、执行模式、导入导出和高级连招继续留到 P7/P8。

前置条件：

- [已验证] P1-P5 基础多点第一版稳定。

## 12. P7：多配置悬浮执行区

目标：

- 支持加载多个配置到悬浮执行区。
- 每个配置对应一个悬浮执行控件。
- 同一时间只执行一个配置任务。

阶段边界：

- P7 必须拆成小阶段推进，避免一次性混入模型、App 页面、Android Overlay、调度和真机验收。
- P7.1 只做 Flutter 侧已加载配置列表、持久化和配置管理页入口，不创建多个悬浮执行控件。
- P7.1 不改变 Android MethodChannel，不影响当前 active profile 的基础多点执行。
- P7.2.1 只做多配置执行控件基础显示，不绑定真实任务执行。

计划任务：

- [已完成] P7.1 新增 `multi_point.loaded_profile_ids_json`。
- [已完成] P7.1 新增已加载 profile 状态模型，默认加载当前 active profile。
- [已完成] P7.1 配置管理页展示已加载/未加载状态。
- [已完成] P7.1 支持加载、卸载 profile，并保证至少保留 1 套已加载配置。
- [已完成] P7.1 删除 profile 时自动收敛已加载列表。
- [已验证] P7.2.1 多配置执行控件基础显示完成真机复验。
- [已验证] P7.2.2 执行控件绑定 profile 真实任务完成真机主路径复验。
- [已验证] P7.2.3 新增执行控件位置持久化，真机复验通过。
- [已验证] P7.2.4 多配置执行异常收尾专项验收通过。
- [已验证] P7.3 执行模式与贴边展开按钮最小范围已完成真机验收。
- [已验证] P7.3.1 多点页执行控件组收起/展开入口和固定默认位置的可恢复入口已通过真机验收。
- [已验证] P7.3.2 展开按钮拖动、位置持久化、横竖屏可见校正和无障碍断开提示复验通过。
- [延期] P7.3 后续再评估执行模式隐藏点位、完整控制条和更细视觉状态。
- [已完成] 执行中其他配置控件拒绝启动，并给出已有任务运行提示。
- [已完成] 正在执行的控件再次点击停止任务。

验收记录：

- [已完成] 2026-05-10，`flutter analyze` 通过。
- [已完成] 2026-05-10，`flutter test` 通过，51 个测试通过。
- [已完成] 2026-05-10，`flutter build apk --debug` 通过。
- [已完成] 2026-05-10，P7.2.2 自动化验证通过：`flutter analyze`、`flutter test`（53 个测试通过）、`flutter build apk --debug`；debug 包已通过 `adb install -r` 安装到在线设备。
- [已验证] 2026-05-10，P7.2.2 真机主路径复验通过：控件 A 执行 A、A 运行中点击 B 被拒绝、再次点击 A 停止、停止后可执行 B，单点模式最小回归通过。
- [已完成] 2026-05-10，P7.2.3 自动化验证通过：`flutter analyze`、`flutter test`（57 个测试通过）、`flutter build apk --debug`；debug 包已通过 `float_clicker_verify` MCP 安装到在线设备。
- [已验证] 2026-05-10，P7.2.3 真机复验通过：拖动后关闭重开恢复位置、刷新 loaded profiles 不跳回默认位置、多个控件位置互不覆盖、横竖屏后仍可见并保持校正后位置。
- [已验证] 2026-05-10，P7.2.4 多配置执行异常收尾专项验收通过：关闭执行控件预览、关闭无障碍快捷开关、撤销悬浮窗权限、隐藏当前执行配置后，任务均安全结束；恢复权限或服务后可再次执行，未见崩溃。
- [已完成] 2026-05-10，P7.3.1 自动化验证通过：`flutter analyze`、`flutter test`（59 个测试通过）、`flutter build apk --debug`、`git diff --check`。
- [已验证] 2026-05-10，P7.3.1 真机验收通过：RMX5080 / Android 16 / debug 包，多人并行测试表现一致；收起、恢复显示、关闭重开默认展开、运行中恢复停止入口和 P7.2.4 异常收尾回归均通过。
- [已完成] 2026-05-10，P7.3.2 自动化验证通过：`dart format`、`flutter analyze`、`flutter test`（61 个测试通过）、`flutter build apk --debug`、`git diff --check`。
- [已验证] 2026-05-10，P7.3.2 真机主路径通过：恢复入口可拖动，关闭重开后位置恢复，横竖屏后保持可见，运行中可恢复并停止任务，P7.2.4 异常收尾无回退。
- [已验证] 2026-05-10，P7.3.2 无障碍断开提示复验通过：基础多点和多配置执行控件运行中关闭无障碍后任务停止，并出现“无障碍服务已断开，点击任务已结束”；重新开启无障碍后可再次执行，未见崩溃或闪退。
- [已验证] 2026-05-11，P7 收尾复盘完成：P7.1 到 P7.3.2 核心能力均已闭环，P7 总阶段推进为已验证。

风险与未完成项：

- [已验证] P7.1 配置加载/卸载主流程已在 RMX5080 真机通过；多点和单点悬浮层开启/关闭完成最小回归。
- [已验证] P7.2.2 profile 绑定执行、单任务互斥、停止和单点最小回归已通过真机主路径复验。
- [已验证] P7.2.3 已修复隐藏同一控件后位置丢失问题，真机确认拖动后关闭重开恢复位置、刷新不跳位、横竖屏仍可见。
- [已验证] P7.2.4 只验证已存在的多配置执行异常收尾能力，不包含贴边展开或执行模式 UI。
- [已验证] P7.3.1 已完成自动化验证和真机验收；恢复入口第一版不支持拖动、位置持久化和横竖屏专项校正。
- [已验证] P7.3.2 已完成恢复入口拖动、位置持久化、横竖屏可见校正和无障碍断开提示复验；P7 核心范围已可收尾。
- [延期] 防误触、贴边吸附、暂停/继续控件、多任务并行和 P8 高级连招不属于 P7 已验证核心范围，后续单独拆阶段。
- [延期] P8 高级连招继续不进入 P7。

### 12.1 P7.1：加载配置列表

目标：

- 先建立“哪些 profile 被加载到未来悬浮执行区”的 Flutter 侧数据基础。
- 让用户在配置管理页选择加载或卸载配置。
- 为 P7.2 多执行控件 Overlay 提供稳定持久化输入。

任务：

- [已完成] 新增 `LoadedMultiPointProfile`。
- [已完成] `MultiPointProfileState` 纳入 `loadedProfiles`，并集中归一化缺失、重复和空列表。
- [已完成] `MultiPointSettingsStore` 读写 `multi_point.loaded_profile_ids_json`。
- [已完成] 配置管理页展示已加载数量和单个 profile 加载状态。
- [已完成] 配置管理页支持加载、卸载 profile。
- [已完成] 未启用任何点位的 profile 拒绝加载。
- [已完成] 已加载列表至少保留 1 个 profile。
- [已完成] 自动化测试覆盖默认加载、持久化、删除收敛、坏 JSON 兜底和页面加载/卸载。
- [已验证] RMX5080 / Android 16 API 36 真机验证默认配置已加载、新建配置未加载、加载后计数变 2、卸载后计数回 1。
- [已验证] 删除测试配置后自动回到默认配置，并保持 1 套配置、1 套已加载。

暂不做：

- [延期] 多个悬浮执行控件。
- [延期] 加载 profile 到 Android Overlay。
- [延期] 执行模式、贴边展开按钮和执行控件位置。
- [延期] 并行任务和高级连招。

前置条件：

- [已验证] P6 多配置管理完成。

### 12.2 P7.2.1：多配置执行控件基础显示

目标：

- 将 P7.1 已加载配置同步到原生侧。
- 显示多个可区分的配置执行控件。
- 先验证悬浮窗口展示、关闭和横竖屏边界，不接入真实任务执行。

任务：

- [已完成] 新建协作记录 `P7.2.1 多配置执行控件基础显示计划.md`。
- [已完成] Flutter 侧组装已加载配置快照。
- [已完成] 新增最小 MethodChannel 展示、更新和关闭方法。
- [已完成] Android 侧创建多个配置执行控件。
- [已完成] 控件点击仅提示下一阶段接入执行。
- [已完成] 支持单个 profile 执行控件显示/隐藏，并在预览层开启时即时刷新。
- [已验证] 真机验证多控件展示、单控件开关、关闭、拖动和横竖屏可见。

暂不做：

- [延期] 绑定 profile 真实执行。
- [延期] 执行中其他控件置灰。
- [延期] 正在执行控件再次点击停止任务。
- [延期] 贴边展开按钮、执行模式和控件位置持久化。

### 12.3 P7.2.2：执行控件绑定配置任务

目标：

- 点击某个执行控件时，执行该控件绑定的 `MultiPointProfile`。
- 同一时间只允许一个 profile 任务运行。
- 当前运行控件再次点击时，结束当前任务。

任务：

- [已完成] 新建协作记录 `P7.2.2 执行控件绑定配置任务计划.md`。
- [已完成] Flutter 侧向原生同步已显示 profile 的点位和点击参数快照。
- [已完成] Android 侧按 `profileId` 保存 profile 执行快照。
- [已完成] 控件点击启动绑定 profile 的多点调度。
- [已完成] 运行中点击其他控件拒绝启动并提示已有任务运行。
- [已完成] 当前运行控件再次点击结束任务。
- [已完成] 关闭预览层、无障碍断开和权限撤销时安全结束任务。
- [已验证] 真机确认控件绑定 profile 的真实点击落到底层应用。

暂不做：

- [延期] 多任务并行执行。
- [延期] 暂停/继续控件。
- [延期] 贴边展开按钮和执行模式。
- [延期] P8 高级连招。

### 12.4 P7.2.3：执行控件位置持久化

目标：

- 用户拖动多配置执行控件后，下次刷新或重新开启执行控件预览时恢复到上次位置。
- 位置持久化绑定 loaded profile，不污染 `MultiPointProfile` 点位坐标。

任务：

- [已完成] 新建协作记录 `P7.2.3 执行控件位置持久化计划.md`。
- [已完成] Flutter 侧新增已加载执行控件位置模型并随 loaded profile 持久化。
- [已完成] Flutter 侧把控件位置随 loaded profile 快照传给 Android。
- [已完成] Android 侧优先使用 Flutter 传入位置，拖动结束后回传 profileId 和坐标。
- [已完成] Flutter 接收位置回调并保存。
- [已完成] 隐藏同一控件时保留按钮位置，重新显示时恢复上次位置。
- [已完成] 自动化测试覆盖位置持久化、MethodChannel 参数和隐藏后再显示的回归场景。
- [已验证] 真机确认关闭重开、刷新、横竖屏后的执行控件位置恢复。

暂不做：

- [延期] 贴边吸附和贴边展开按钮。
- [延期] 控件分组、面板折叠和滚动列表。
- [延期] 执行中暂停/继续按钮。
- [延期] P8 高级连招。

### 12.5 P7.2.4：多配置执行异常收尾专项验收

目标：

- 在不新增 UI 的前提下，专项验证 P7.2 多配置执行真实链路的异常收尾稳定性。
- 确认关闭预览层、无障碍断开、悬浮窗权限撤销、隐藏当前执行配置时，任务都会安全结束。

任务：

- [已完成] 新建协作记录 `P7.2.4 多配置执行异常收尾专项验收.md`。
- [已验证] 运行某个配置时关闭执行控件预览，任务停止，控件消失，可重新开启预览并再次执行。
- [已验证] 运行中关闭无障碍快捷开关，任务停止，重新开启无障碍后可再次执行。
- [已验证] 运行中撤销悬浮窗权限，执行控件关闭，任务停止，恢复权限后可重新开启预览。
- [已验证] 运行中隐藏当前执行配置，当前任务停止，其他执行控件仍可用。
- [已验证] 多人并行测试反馈表现一致通过，未发现崩溃、闪退或任务残留。

暂不做：

- [延期] 贴边展开按钮。
- [延期] 执行模式 UI。
- [延期] 暂停/继续控件。
- [延期] 多任务并行。
- [延期] P8 高级连招。

### 12.6 P7.3：执行模式与贴边展开按钮

目标：

- 在 P7.2 多配置执行链路稳定后，补齐执行模式的收起/展开入口。
- P7.3.1 已完成自动化验证和真机验收。
- 第一小步 P7.3.1 只做执行控件组收起/展开入口和停止入口恢复，不扩大到防误触增强。

任务：

- [已完成] 新建协作记录 `P7.3 执行模式与贴边展开按钮计划.md`，记录计划草案。
- [已验证] P7.3.1 多点页“执行控件”卡片提供全局收起/展开入口，不做单 profile 独立收起。
- [已验证] P7.3.1 Android 收起时隐藏多个执行控件，只保留固定默认位置的小型恢复入口。
- [已验证] P7.3.1 点击恢复入口后重新显示已加载执行控件。
- [已验证] P7.3.1 运行中收起后仍可通过恢复入口找回当前运行控件，并停止任务。
- [已验证] P7.3.1 收起状态不持久化，关闭并重新开启预览时默认展开；没有可见 loaded profile 时不显示恢复入口。
- [已验证] P7.3.2 展开按钮支持拖动、位置持久化和横竖屏可见区域校正，真机验收通过。
- [已验证] P7.3.2 无障碍断开 Toast 提示补强通过真机复验。
- [已验证] P7.3.1 真机验收通过。

暂不做：

- [延期] 暂停/继续控件。
- [延期] 多任务并行。
- [延期] 透明度、锁定位置、长按执行和二次确认。
- [延期] 控件分组、折叠面板滚动列表和复杂执行模式。
- [延期] P8 高级连招。

## 13. P8：高级连招能力

目标：

- 支持 tap / longPress / swipe / delay。
- 支持动作列表编辑和复杂编排。

计划任务：

- [延期] 设计高级连招入口。
- [延期] 动作列表 UI。
- [延期] 动作排序、复制、删除。
- [延期] 长按参数编辑。
- [延期] 滑动起止点编辑。
- [延期] 延迟动作编辑。
- [延期] 导入导出或方案保存。

前置条件：

- [未开始] 通用动作调度内核稳定。
- [未开始] 根据基础多点和多配置真实使用反馈确认复杂动作需求。

## 14. 设计调整记录

| 日期 | 调整内容 | 原因 | 影响范围 |
| --- | --- | --- | --- |
| 2026-05-08 | 基础多点第一版明确只做多个固定点位按顺序点击 | 控制复杂度，保护单点稳定性 | 多点模型、页面、Android 调度 |
| 2026-05-08 | 暂停态允许完整编辑，点位结构变化后从当前轮第一个启用点位继续 | 兼顾用户修正需求和调度器可控性 | Flutter 页面、调度进度语义 |
| 2026-05-08 | 后期新增多配置执行设计文档，不进入基础多点第一版 | 避免多配置执行扩大当前阶段范围 | 文档、阶段计划 |
| 2026-05-08 | P1 完成 Flutter 多点模型与持久化，运行时重排按当前列表顺序，JSON 读取按保存的 `order` 排序 | 支持页面编辑直接复用模型约束，同时保证持久化恢复顺序稳定 | Flutter 模型、持久化、测试 |
| 2026-05-08 | P2 先完成 Flutter 页面、本地编辑和持久化；首页全局 active mode、权限不足、模式冲突和暂停结构变化标记延后到 MethodChannel/Android 多点快照接入 | P2 不伪造原生状态，避免破坏单点稳定运行状态 | 首页、多点页面、阶段边界、测试 |
| 2026-05-08 | P3 拆分为 P3.1-P3.6，先做协议空壳和单个多点点位组件，再逐步接入 Manager、控制组件、位置回传和边界处理 | Android Overlay 涉及窗口、组件复用、互斥和横竖屏，原 P3 粒度过大，容易把调度执行混入 Overlay 阶段 | Android MethodChannel、Overlay 组件、阶段计划 |
| 2026-05-08 | P3.1/P3.2 完成 Android 多点 Overlay 协议、快照、单个编号点位组件和 Flutter 调用接入；多点执行仍返回未实现 | 先验证原生窗口和协议，不提前引入多点调度复杂度 | Android MethodChannel、Flutter 多点页、Overlay 点位组件 |
| 2026-05-08 | 修正 P3.1/P3.2 检查发现的全禁用点位边界：多点模式开启状态不再依赖点位组件是否显示，全部禁用时保持模式开启但不显示点位 | 符合“允许禁用全部点位，执行前拒绝”的既定语义，避免快照误判 `modeEnabled=false` | Android 多点 Overlay Manager、快照语义 |
| 2026-05-08 | P3.3 完成多点 Overlay Manager 基础版，从单个点位升级为多个启用点位组件，禁用点位不显示，启用点位连续编号 | 满足基础多点悬浮层展示需求，同时仍不进入点击调度 | Android MultiPointOverlayManager、多点点位组件 |
| 2026-05-08 | P3.4 完成多点 Overlay 复用单点控制条、收起图标和独立执行控件，支持普通、简洁、极简交互；任务按钮只提示调度未实现 | 复用已稳定悬浮交互，保持 P3 不进入真实点击调度 | Android MultiPointOverlayManager、悬浮交互组件 |
| 2026-05-08 | P3.5 完成多点点位位置专用回调和 Flutter 持久化；控制组件位置继续通过 Overlay 快照保存 | 点位拖动需要明确事件协议，控制组件位置已有完整快照链路，避免重复协议扩大范围 | Android MethodChannel、Flutter 多点页、多点持久化 |
| 2026-05-08 | P3.6.1 完成多点 Overlay 横竖屏和显示边界刷新，目标点与控制组件越界后裁剪回可见区域并回传持久化 | 复用单点横竖屏稳定策略，避免多点窗口在配置变化后停留在旧屏幕坐标 | Android MultiPointOverlayManager、MainActivity |
| 2026-05-08 | P3.6.2 复查并硬化关闭多点模式收尾：关闭时移除目标点、控制条、收起图标、独立控件，注销显示监听，并取消待执行的显示边界刷新 | 多点关闭必须只留下稳定快照，避免延迟刷新或复用控制条语义导致窗口残留和状态误导 | Android MultiPointOverlayManager、ToolbarOverlayComponent |
| 2026-05-08 | P3.6.3 复查并硬化单点/多点互斥、权限撤销和窗口异常错误码；多点窗口创建任一失败时回滚已创建窗口并返回 `overlay_window_unavailable`，权限撤销时统一退出多点模式 | 避免半创建状态、权限撤销无提示或两个模式同时显示；P3 仍不接入真实点击调度 | MainActivity、MultiPointOverlayManager、Overlay 组件 |
| 2026-05-08 | P3 Android 多点 Overlay 自动化回归通过，阶段状态推进为已完成；P4 拆分为 P4.1-P4.4 | P3 已完成多点悬浮显示、控制组件复用、位置回传、横竖屏、互斥和异常边界；P4 需要单独分步接入真实调度，避免大规模重写 | 阶段计划、后续调度实现边界 |
| 2026-05-09 | P4.1 完成 tap-only `AutomationAction` 和 `AccessibilityGestureExecutor`，单点调度器改为通过执行器派发点击；手势成功、取消、拒绝派发和服务断开结果统一返回调度器处理 | 为 P4.2 多点顺序调度提供可复用的底层执行入口，同时把单点手势异常收尾集中到同一层 | Android AutomationAction、AccessibilityGestureExecutor、SinglePointClickScheduler、FloatClickerAccessibilityService |
| 2026-05-09 | P4.2 完成 `MultiPointClickScheduler` 基础版，按启用点位顺序生成 tap 动作，支持有限轮次、无限循环、暂停、继续、结束和多点进度字段 | 先完成独立调度内核，不直接接入 MethodChannel，避免 P4.2 和 P4.3 职责混在一起 | Android MultiPointClickScheduler、FloatClickerAccessibilityService |
| 2026-05-09 | P4.3 完成多点三态控制接入，`start/pause/resume/endMultiPointClicking` 接入原生调度器，多点快照和悬浮控制组件跟随任务状态刷新；Flutter 多点页补齐执行、暂停、继续、结束入口 | 打通 P4.2 调度器到 MethodChannel、Overlay 控制组件和 Flutter 页面，先保持 P4.4 的暂停编辑语义不混入本阶段 | Android MainActivity、MultiPointOverlayManager、MultiPointTargetOverlayComponent；Flutter AndroidPermissionService、MultiPointPage |
| 2026-05-09 | P4.4 完成暂停中点位结构变化重建动作序列，以及暂停中有限循环次数达到上限时继续即结束的语义；Flutter 侧继续任务改为以原生快照为准 | 让暂停编辑后的继续行为和新循环次数语义都由原生调度器统一裁定，避免 Flutter 乐观状态与真实状态偏离 | Android MultiPointClickScheduler、MultiPointOverlayManager；Flutter MultiPointPage、测试 mock |
| 2026-05-09 | P5 排查并修复无障碍连接态同步：`accessibilityGranted` 与 `accessibilityConnected` 统一走原生状态入口；首页、单点页、多点页在前台恢复时增加短时重试收敛，并避免首帧未连接时把原生 `running/paused` 错误降回 `idle` | 真机专项验收中发现首页卡片、页面状态与 `dumpsys accessibility` 存在间歇性脱节，已影响 P5 验收可信度，需要先收敛状态同步再继续专项复验 | MainActivity、AccessibilityServiceStateBus、AccessibilityGestureExecutor；Flutter AndroidPermissionService、HomePage、ClickerPage、MultiPointPage、测试 |
| 2026-05-09 | P5 补强无障碍服务实例生命周期：系统服务组件名改为按 `ComponentName` 解析比对，服务实例在 `onCreate/onServiceConnected/onUnbind/onDestroy` 中幂等维护 | 回退后真机出现系统 `Enabled + Bound`、首页却显示“服务未连接”的不同步；需要避免包更新或重新绑定后 App 内服务实例状态丢失 | AccessibilityServiceStateHelper、FloatClickerAccessibilityService |
| 2026-05-09 | P6 完成多配置管理配置库：新增 `MultiPointProfile`、profile 持久化、旧单配置迁移、多点页配置管理入口和自动化回归 | P5 已验证后进入后续能力第一步；先只做 App 内 profile 管理，避免把 P7 多执行控件和 P8 高级连招混入 P6 | Flutter 多点模型、持久化、页面、测试、阶段文档 |
| 2026-05-09 | P6 重命名配置真机问题收敛，并记录 P6 计划粒度偏大，后续阶段需拆成更小任务推进 | 真机发现弹窗、菜单和输入法视口变化同帧触发 Flutter 构建作用域断言；同时 P6 一次性覆盖模型、迁移、页面、测试和文档，反馈粒度过大 | Flutter 多点配置管理页、协作记录、后续阶段计划方式 |
| 2026-05-09 | 修复基础多点执行点击无效：运行中多点目标窗口改为不可触摸，暂停或结束后恢复可拖动 | P6 分支验收时发现多点任务状态执行但底层应用没有点击效果；多点目标点运行中未像单点一样透传触摸，点击可能被悬浮点自身消费 | MultiPointTargetOverlayComponent、MultiPointOverlayManager、真机验收 |
| 2026-05-10 | P7 拆分出 P7.1 加载配置列表，仅实现 Flutter 侧 loaded profile 数据、持久化和配置管理页入口 | 遵循 P6 复盘后的“小阶段推进”要求，先建立多配置执行区的数据基础，不提前引入 Android 多执行控件和执行模式 | Flutter 多点模型、持久化、配置管理页、测试、阶段文档 |
| 2026-05-10 | P7.1 加载配置列表完成 RMX5080 真机验收，配置加载/卸载/删除收敛通过，多点和单点悬浮层开启关闭完成最小回归 | P7.1 只改变 Flutter loaded profile 数据和配置管理页，不进入 Android 多执行控件；真机重点验证 App 内状态和旧能力入口不回退 | Flutter 配置管理页、阶段文档、协作记录 |
| 2026-05-10 | P7.2.1 计划拆为多配置执行控件基础显示，只做多个控件的原生悬浮展示和关闭，不绑定真实执行 | 继续落实“小阶段推进”，先拆出窗口展示风险，避免同时改 MethodChannel、Overlay、调度器和执行状态 | Flutter 平台服务、Android Overlay、阶段文档、协作记录 |
| 2026-05-10 | P7.2.1 完成自动化验证：新增多配置执行控件预览入口、MethodChannel、原生 manager 和独立控件组件，点击仅提示下一阶段接入执行 | 先验证多控件悬浮窗口基础能力，不提前进入 profile 真实执行、任务互斥和位置持久化 | Flutter 平台服务、多点页、Android MultiProfileExecutionOverlayManager、测试、阶段文档 |
| 2026-05-10 | P7.2.1 根据真机反馈补充单个执行控件显示/隐藏开关，预览层开启时可即时刷新单个 profile 控件 | 原全局开启/关闭颗粒度过粗，无法单独控制某个配置控件；本轮仍只做显示状态，不进入真实执行 | Flutter 多点页、loaded profile 持久化、测试、协作记录 |
| 2026-05-10 | P7.2.1 真机复验通过，P7.2.2 拆为执行控件绑定配置任务 | 控件显示、单控件开关、拖动和横竖屏已稳定；下一步只接入单任务真实执行，不进入并行、暂停/继续和高级动作 | 阶段文档、协作记录、后续 Flutter/Android 执行链路 |
| 2026-05-10 | P7.2.2 完成执行控件绑定 profile 真实任务的自动化验证 | 多配置执行控件已可传递完整 profile 快照并复用多点调度器，当前控件再次点击停止，其他控件运行中拒绝启动；仍需真机确认点击落点和回归 | Flutter 平台服务、多点页、Android MultiProfileExecutionOverlayManager、测试、协作记录 |
| 2026-05-10 | P7.2.2 完成真机主路径复验，P7.2.3 拆为执行控件位置持久化 | 多配置控件真实执行、单任务互斥、停止和单点最小回归已通过；下一步只保存拖动后的控件位置，不进入贴边展开、暂停继续和高级动作 | 阶段文档、协作记录、Flutter/Android MethodChannel |
| 2026-05-10 | P7.2.3 完成执行控件位置持久化自动化验证 | 已加载执行控件会保存拖动后的逻辑坐标，重新开启预览层时传给 Android 恢复位置；仍需真机确认关闭重开、刷新和横竖屏表现 | Flutter 多点模型、持久化、MethodChannel、Android 多配置执行控件 |
| 2026-05-10 | 修复 P7.2.3 隐藏同一执行控件后位置丢失 | 真机复验发现隐藏控件再重新显示时会回到默认位置；原因是隐藏时移除了 loaded profile 条目，已改为保留隐藏条目和按钮坐标 | Flutter 多点模型、配置管理页、多点页、测试 |
| 2026-05-10 | P7.2.3 执行控件位置持久化完成真机复验 | 拖动后关闭重开恢复位置、刷新 loaded profiles 不跳回默认位置、多个控件位置互不覆盖、横竖屏后仍可见并保持校正后位置均通过 | 阶段文档、协作记录 |
| 2026-05-10 | P7.2.4 多配置执行异常收尾专项验收通过 | 多人并行测试确认关闭执行控件预览、无障碍断开、悬浮窗权限撤销、隐藏当前执行配置后任务均安全结束，恢复后可再次执行，未见崩溃 | 阶段文档、协作记录 |
| 2026-05-10 | 校正 P7.3 阶段状态和验收节奏 | P7.3 只有计划草案，贴边展开按钮和执行模式尚未实现，不能提前进入真机测试；先把未实现能力从验收项中拆出 | 阶段文档、协作记录 |
| 2026-05-10 | P7.3.1 完成执行控件组收起/展开最小实现 | 多点页新增全局收起入口，Android 收起时只保留固定默认位置恢复入口；收起状态不持久化，位置持久化和横竖屏校正顺延 P7.3.2 | Flutter 多点页、MethodChannel、Android 多配置执行控件、测试 |
| 2026-05-10 | P7.3.1 执行控件组收起/展开完成真机验收 | RMX5080 / Android 16 / debug 包多人并行测试表现一致通过，覆盖收起、恢复显示、关闭重开默认展开、运行中恢复停止入口和 P7.2.4 异常收尾回归 | 阶段文档、协作记录 |
| 2026-05-10 | P7.3.2 完成恢复入口拖动、位置持久化和横竖屏校正自动化验证 | 恢复入口坐标作为全局执行区 UI 状态独立持久化，不绑定 profile；Android 侧拖动和边界校正后回传 Flutter 保存，收起状态仍不持久化 | Flutter 多点页、持久化、MethodChannel、Android 恢复入口、测试 |
| 2026-05-10 | P7.3.2 真机反馈补强无障碍断开提示 | 主路径验收通过，但运行中撤销无障碍权限缺少“无障碍服务已断开，点击任务已结束”提示；已补强单点、基础多点和多配置执行的断开提示保留逻辑 | Android 单点、多点、多配置执行 Overlay Manager、协作记录 |
| 2026-05-10 | P7.3.2 完成真机复验并推进为已验证 | T6-T7 确认基础多点和多配置执行控件运行中关闭无障碍后任务停止、提示出现、恢复后可再次执行；P7.3.2 当前范围验收完成 | 阶段文档、协作记录、后续阶段节奏 |
| 2026-05-11 | P7 核心能力收尾并推进为已验证 | P7.1 到 P7.3.2 已覆盖多配置加载、多执行控件、profile 真实执行、位置持久化、收起恢复入口和异常收尾；防误触、贴边吸附和暂停/继续控件后续另拆小阶段 | 阶段文档、AI 交接提示词、后续阶段节奏 |

## 15. 当前下一步

P7 核心能力已完成真机验收，当前推荐下一步：

1. 等待多配置悬浮执行区真实使用反馈，或进入新的 P7.3.3 计划草案。
2. 如果进入 P7.3.3，建议二选一：只做恢复入口贴边吸附，或只做防误触最小开关；不要同时做多个增强。
3. P7.3.3 需要先写计划、范围和验收标准，功能实现完成后再发真机测试模板。
4. 暂不进入暂停/继续控件、多任务并行和 P8 高级连招。
