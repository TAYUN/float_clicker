# 悬浮窗横竖屏贴边问题

## 背景

单点模式的点击点、控制条、收起图标和独立执行控件都是 Android `WindowManager` 悬浮窗。用户会在 Float Clicker 竖屏页面开启单点模式，然后切换到其他竖屏或横屏 App 中拖动悬浮组件。

## 问题现象

- 从竖屏切换到横屏应用后，悬浮点或控制条有时无法继续向右拖动，常见表现是距离右边还有约 2/5 屏宽时就被卡住。
- 如果简单统一放宽右边界，竖屏下点位可能越过右侧边界，且下边界可能按短边计算导致距离底部约 2/5 屏就被卡住。
- 早期修复中，横竖屏后首次拖动需要拖很远，控件才会从屏幕边缘脱离。
- 尝试用 `getLocationOnScreen()` 同步 `WindowManager.LayoutParams.x/y` 后，控件虽然能立即移动，但一拖动就会瞬间跳到手指右下方。
- 使用动态 `rootWindowInsets` 避让系统栏时，不同悬浮组件拿到的 inset 不一致，导致控制条有时避让状态栏，而悬浮点不避让。

## 原因分析

- `WindowManager.LayoutParams.x/y` 和 `View.getLocationOnScreen()` 不属于完全相同的坐标语义，不能直接互相写回。
- 切到其他横屏 App 时，`context.resources.displayMetrics` 有时仍反映 Float Clicker 自身的竖屏配置，用它计算边界会把横屏拖动范围误限制成竖屏宽度。
- 悬浮窗在其他 App 上方运行时，Activity 的 `onConfigurationChanged()` 不一定覆盖所有显示变化，所以仅依赖 Activity 配置变化刷新不够。
- 系统状态栏、导航栏和刘海区的可见性在不同 App 中差异较大，如果每个 overlay view 动态读取 inset，容易出现组件之间规则不一致。

## 最终策略

- 悬浮窗使用整屏布局参数：
  - `FLAG_LAYOUT_IN_SCREEN`
  - `FLAG_LAYOUT_NO_LIMITS`
  - Android P+ 设置 `LAYOUT_IN_DISPLAY_CUTOUT_MODE_SHORT_EDGES`
- 边界尺寸优先读取悬浮窗 WindowContext 的 `WindowMetrics`：
  - Android 11+ 使用 `createWindowContext(TYPE_APPLICATION_OVERLAY, null)`
  - 通过 `maximumWindowMetrics.bounds` 获取更接近悬浮窗实际显示区域的尺寸
- 旧版本或读取失败时再读取默认 Display 的实时尺寸：
  - `DisplayManager.getDisplay(Display.DEFAULT_DISPLAY)`
  - `display.getRealSize(Point)`
  - 再结合 `display.rotation` 校正宽高方向：横屏强制长边为宽、短边为高
  - 只有读取失败时才回退到 `context.resources.displayMetrics`
- 竖屏采用确定性边界：
  - 左边界为 `0`
  - Android 11+ 顶部通过 `WindowMetrics.windowInsets` 避让状态栏
  - 旧版本才回退到系统 `status_bar_height`
- 右/下边界按当前窗口尺寸限制，避免竖屏点位越出右侧或提前卡住下方
- 跨应用横屏拖动时采用动态放宽：
  - 如果手指 `rawX` 已超过旧竖屏宽度，说明用户正在横屏右侧区域拖动
  - 此时右边界放宽到当前可推断的屏幕长边宽度，避免卡在屏幕中间，同时不能越过横屏右侧
  - 下边界使用当前可推断屏幕短边作为横屏高度，避免横屏下方越界
- 拖动开始、拖动过程中、配置回放时都走同一套边界计算，避免不同入口产生不同位置规则。
- 单点和多点 OverlayManager 都注册 `DisplayManager.DisplayListener`，默认屏幕变化时主动刷新悬浮组件位置。
- `Activity.onConfigurationChanged()` 同时通知单点和多点 Manager，覆盖应用自身旋转场景。

## 踩坑记录

- 不要把 `getLocationOnScreen()` 的结果直接写回 `WindowManager.LayoutParams.x/y`，会产生坐标系偏移，拖动时控件会跳位。
- 不要只用 `context.resources.displayMetrics` 判断横竖屏和边界，它在切到其他 App 后可能不是当前默认 Display 的真实方向。
- 不要只看 `display.getRealSize(Point)` 的 `x/y` 顺序；跨应用横屏时部分设备仍可能返回竖屏方向，需要结合 `display.rotation` 校正。
- 不要在所有场景统一放宽右边界，否则竖屏点位会越出右侧；右边界放宽只应在拖动进入旧竖屏宽度之外时启用。
- 不要把下边界固定成短边；这只适合已确认的横屏拖动场景，竖屏应按长边限制。
- 不要让不同悬浮组件各自动态决定是否避让状态栏，容易出现点击点和控制条规则不一致。

## 回归检查项

- 竖屏开启悬浮层后，点位、控制条、收起图标、极简按钮不能越过右边界。
- 竖屏拖动到下方时，不能在距离底部约 2/5 屏处提前卡住。
- 从 Float Clicker 竖屏切到其他横屏应用后，向右拖动不能在屏幕中间提前卡住。
- 横屏拖动时，下边界不能越出屏幕底部。
- 单点和多点的普通、简洁、极简三种交互模式都要覆盖。

## 当前代码位置

- `android/app/src/main/kotlin/com/example/float_clicker/OverlayWindowHelper.kt`
  - `overlayParams()`
  - `coercePosition()`
  - `coerceParamsToVisibleBounds()`
  - `overlayBoundsPx()`
  - `realScreenSizePx()`
- `android/app/src/main/kotlin/com/example/float_clicker/SinglePointOverlayManager.kt`
  - `DisplayManager.DisplayListener`
  - `handleDisplayBoundsChanged()`
- `android/app/src/main/kotlin/com/example/float_clicker/MultiPointOverlayManager.kt`
  - `DisplayManager.DisplayListener`
  - `handleDisplayBoundsChanged()`
- `android/app/src/main/kotlin/com/example/float_clicker/MainActivity.kt`
  - `onConfigurationChanged()`
