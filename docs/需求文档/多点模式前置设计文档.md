# 多点模式前置设计文档

## 1. 背景与目标

Float Clicker 当前单点模式已经完成三态任务调度、普通/简洁/极简悬浮交互、全局悬浮外观配置和主要异常收尾。下一阶段准备进入多点模式设计。

多点模式不能简单理解为“复制多个单点”。它需要解决多个点位的管理、执行顺序、任务进度、悬浮组件复用、后续长按/滑动/连招扩展等问题。因此本设计先明确两条产品线边界：

- 基础多点模式：面向普通用户，解决多个固定位置按顺序自动点击。
- 高级连招模式：面向复杂自动化，支持点击、长按、滑动、延迟等动作编排。

本阶段目标是先设计基础多点，并在底层模型上为高级连招预留扩展，不在第一版多点中直接堆复杂动作 UI。

## 2. 设计结论

建议采用“两层能力，一套执行内核”的方案：

```text
基础多点模式
- 用户管理多个点击点
- 按点位顺序执行 tap
- 使用简单设置：间隔、次数、无限循环、点击持续时间

高级连招模式
- 用户编辑动作列表
- 动作可以是 tap / longPress / swipe / delay
- 支持更复杂的编排、命名、导入导出等后续能力

共享执行内核
- 使用 AutomationAction 描述可执行动作
- 单点、多点、连招最终都转换为动作序列执行
```

这样第一版不会把多点页面做得过重，但以后增加长按、滑动和复杂连招时，不需要推翻 Android 调度器。

## 3. 功能边界

### 3.1 基础多点模式包含

- 开启和关闭多点模式。
- 新增、删除、拖动多个点击点。
- 点击点显示序号，例如 `1 / 2 / 3`。
- 点击点位置持久化。
- 支持启用或禁用单个点位。
- 按点位顺序依次点击。
- 支持点击间隔、循环次数、无限循环、点击持续时间。
- 支持任务三态：`idle / running / paused`。
- 支持执行、暂停、继续、结束任务。
- 支持普通、简洁、极简三种悬浮交互模式。
- 复用全局悬浮外观配置。
- 复用悬浮窗异常收尾、横竖屏贴边、权限检查等单点稳定性策略。

### 3.2 基础多点模式暂不包含

- 单个点位配置不同的点击间隔。
- 单个点位配置不同的点击次数。
- 长按动作。
- 滑动动作。
- 复杂延迟动作。
- 条件判断、图像识别、找色。
- 录制用户轨迹。
- 脚本导入导出。
- 多套方案管理。
- 动作分组、嵌套循环、随机化执行。

这些能力应进入高级连招模式，而不是挤进基础多点第一版。

### 3.3 高级连招模式包含方向

高级连招模式用于表达复杂自动化动作，建议后续独立入口或从多点模式升级进入。

动作类型建议：

```text
AutomationAction
- tap(targetId, durationMs)
- longPress(targetId, durationMs)
- swipe(fromTargetId, toTargetId, durationMs)
- delay(durationMs)
```

后续可扩展：

- 动作命名。
- 动作复制、删除、拖拽排序。
- 每个动作独立配置执行后延迟。
- 动作序列重复。
- 方案保存和切换。
- 导入导出。

高级连招不建议作为多点第一版主流程，因为它会显著增加 UI、状态和验收复杂度。

## 4. 用户场景

### 4.1 基础多点场景

用户需要反复点击多个固定位置，例如：

- 先点按钮 A，再点按钮 B，再点确认。
- 在游戏或工具中依次点击多个固定控件。
- 重复完成一组固定点击流程。

用户进入多点模式后，添加多个点位，拖到目标位置，点击执行。任务按 `1 -> 2 -> 3` 顺序循环执行。

### 4.2 高级连招场景

用户需要执行更复杂的操作，例如：

- 长按某个位置。
- 从一个位置滑动到另一个位置。
- 点击后等待一段时间，再继续下一步。
- 多个动作组成一套可重复执行的操作链。

这类场景需要动作列表和更强的编辑能力，应由高级连招模式承载。

## 5. 核心模型设计

### 5.1 多点点位

```text
MultiPointTarget
- id
- order
- label
- x
- y
- enabled
```

字段说明：

- `id`：稳定标识，不能因排序变化而改变。
- `order`：执行顺序。
- `label`：展示名称，默认可使用序号。
- `x / y`：点位左上角或中心坐标的持久化数据，具体语义需和 Android overlay 统一。
- `enabled`：是否参与执行。

约束：

- 至少需要 1 个启用点位才能开始任务。
- 删除点位后需要重新整理显示序号，但不要求复用已删除的 `id`。
- 拖动点位过程中，如果任务正在运行，后续执行应使用最新坐标。

### 5.2 多点任务配置

基础多点第一版建议复用单点点击配置语义：

```text
MultiPointSettings
- intervalMs
- repeatCount
- infiniteLoop
- tapDurationMs
```

执行含义：

- `intervalMs`：两个动作之间的间隔。
- `repeatCount`：整组点位序列重复次数。
- `infiniteLoop`：无限循环整组点位序列。
- `tapDurationMs`：每个点位单次点击持续时间。

说明：

- 第一版不支持每个点位独立 interval/duration，避免基础模式 UI 过重。
- 后续如果用户强烈需要单点独立参数，应优先放进高级连招模式。

### 5.3 多点悬浮交互配置

多点模式复用已有 `OverlayInteractionMode`：

```text
OverlayInteractionMode
- normal
- compact
- minimal
```

多点模式需要独立保存组件位置和收起状态：

```text
MultiPointOverlayUiSettings
- interactionMode
- toolbarPosition
- collapsedToolbarPosition
- actionButtonPosition
- isToolbarCollapsed
```

点位位置由 `MultiPointTarget` 保存，不放在 `OverlayUiSettings` 内。

### 5.4 全局悬浮外观配置

多点模式必须复用当前全局配置：

```text
GlobalOverlayAppearanceSettings
- targetPointScale
- toolbarScale
- actionButtonScale
```

含义：

- `targetPointScale`：影响所有多点点位。
- `toolbarScale`：影响完整控制条和收起图标。
- `actionButtonScale`：影响独立执行控件。

多点模式不新增另一套大小配置。

### 5.5 通用动作模型

为了后续高级连招，建议 Android 调度内核先围绕动作抽象设计：

```text
AutomationAction
- id
- type
- targetId
- fromTargetId
- toTargetId
- durationMs
- delayAfterMs
```

基础多点可以转换为动作序列：

```text
targets: [1, 2, 3]

actions:
- tap(targetId: 1, durationMs: tapDurationMs)
- tap(targetId: 2, durationMs: tapDurationMs)
- tap(targetId: 3, durationMs: tapDurationMs)
```

高级连招后续再直接暴露 `AutomationAction` 编辑 UI。

## 6. 执行语义

### 6.1 基础多点执行顺序

有限次数示例：

```text
enabled targets: 1, 2, 3
repeatCount: 2

执行顺序:
1 -> 2 -> 3 -> 1 -> 2 -> 3 -> idle
```

无限循环示例：

```text
enabled targets: 1, 2, 3
infiniteLoop: true

执行顺序:
1 -> 2 -> 3 -> 1 -> 2 -> 3 -> ...直到用户暂停、结束或关闭模式
```

### 6.2 暂停与继续

暂停规则：

- 如果当前没有正在派发的手势，立即进入 `paused`。
- 如果当前手势正在执行，不强行中断手势，等当前手势回调后停止调度下一步。
- 暂停需要保留当前轮次和当前动作索引。

继续规则：

- 从暂停时保留的进度继续。
- 不重新执行已经完成的动作。
- 如果暂停时点位位置发生变化，继续后使用最新点位坐标。

### 6.3 结束任务

结束规则：

- 任务状态进入 `idle`。
- 清空当前轮次和动作索引。
- 悬浮组件保留，除非用户关闭多点模式。
- 下次执行从第一个启用点位开始。

### 6.4 配置变化

任务运行中允许的变化：

- 拖动点位：允许，后续动作使用最新坐标。
- 拖动控制条、收起图标、独立执行控件：允许。
- 切换悬浮交互模式：允许，不结束任务。
- 调整全局悬浮外观：允许，不结束任务。

任务运行中需要谨慎处理的变化：

- 新增点位。
- 删除点位。
- 禁用当前或后续点位。
- 修改点击间隔、次数、无限循环。

第一版建议：

- 运行中允许修改位置和悬浮外观。
- 运行中不允许新增、删除、重排、启禁点位；需要先暂停或结束。
- 暂停中允许编辑点位列表，但继续前需要重新校验是否仍有启用点位。

## 7. 悬浮组件设计

### 7.1 多点点位组件

基础多点需要新的多点点位组件：

```text
MultiPointTargetOverlayComponent
```

职责：

- 展示一个可拖动点位。
- 显示序号或标签。
- 回传拖动后位置。
- 支持选中态或编辑态。

视觉建议：

- 继承单点悬浮点的蓝色圆环语言。
- 用序号区分多个点。
- 保持尺寸受 `targetPointScale` 控制。
- 避免每个点都加入复杂按钮，防止屏幕拥挤。

### 7.2 多点 Overlay Manager

建议新增独立管理类：

```text
MultiPointOverlayManager
- MultiPointTargetOverlayComponent list
- ToolbarOverlayComponent
- CollapsedToolbarComponent
- ActionButtonOverlayComponent
- OverlayInteractionState
```

复用：

- `ToolbarOverlayComponent`
- `CollapsedToolbarComponent`
- `ActionButtonOverlayComponent`
- `OverlayWindowHelper`
- `OverlayAppearanceSettings`
- `OverlayComponentMetrics`

新增：

- 多点点位列表管理。
- 多点点位排序和显示。
- 多点模式状态快照。

### 7.3 控制条行为

基础多点的控制条不需要新增复杂按钮。第一版可复用单点控制语义：

- 执行。
- 暂停。
- 继续。
- 结束任务。
- 关闭多点模式。

点位新增、删除、排序建议先放在 Flutter 多点页面中完成，不在悬浮控制条中展开，以免悬浮 UI 过重。

## 8. Android 手势能力评估

Android `AccessibilityService.dispatchGesture()` 可以支持：

- 点击：短时长 `StrokeDescription`。
- 长按：较长时长 `StrokeDescription`。
- 滑动：使用 `Path` 从起点移动到终点。

因此技术上可以支持长按和滑动。但设计上不建议基础多点第一版直接暴露这些复杂动作。

原因：

- 长按和滑动需要每个动作独立配置 duration。
- 滑动需要两个坐标点或一条路径，编辑成本高于普通点位。
- 复杂动作需要动作列表、排序、参数编辑和更细的验收。
- 如果塞进基础多点，会让普通用户的多点点击流程变复杂。

建议：

- Android 调度器底层为 `AutomationAction` 预留 `longPress / swipe / delay`。
- Flutter 第一版多点页面只生成 `tap` 动作。
- 后续高级连招页面再开放完整动作编辑。

## 9. 持久化设计

### 9.1 基础多点配置 key

```text
multi_point.interval_ms
multi_point.repeat_count
multi_point.infinite_loop
multi_point.tap_duration_ms
```

### 9.2 多点点位 key

建议将点位列表保存为结构化 JSON 字符串：

```text
multi_point.targets_json
```

示例：

```json
[
  {"id":"p1","order":1,"label":"1","x":120.0,"y":300.0,"enabled":true},
  {"id":"p2","order":2,"label":"2","x":260.0,"y":480.0,"enabled":true}
]
```

说明：

- 点位是列表数据，使用单个 JSON key 比 `target_1_x` 这类散 key 更容易迁移。
- 后续增加字段时可做版本兼容。

### 9.3 多点悬浮交互 key

```text
multi_point.overlay_interaction_mode
multi_point.toolbar_position_x
multi_point.toolbar_position_y
multi_point.collapsed_toolbar_position_x
multi_point.collapsed_toolbar_position_y
multi_point.action_button_position_x
multi_point.action_button_position_y
multi_point.is_toolbar_collapsed
```

### 9.4 高级连招配置 key

高级连招后续建议独立命名空间：

```text
combo.profiles_json
combo.last_profile_id
```

不建议把高级连招动作直接塞入 `multi_point.*`，避免基础多点和高级连招互相污染。

## 10. MethodChannel 协议建议

### 10.1 多点模式方法

Flutter 调用 Android：

```text
showMultiPointOverlay
hideMultiPointOverlay
updateMultiPointSettings
updateMultiPointTargets
updateMultiPointOverlayUiSettings
updateGlobalOverlayAppearanceSettings
startMultiPointClicking
pauseMultiPointClicking
resumeMultiPointClicking
endMultiPointClicking
getMultiPointOverlaySnapshot
```

Android 回调 Flutter：

```text
onMultiPointOverlayStateChanged
onMultiPointTargetPositionChanged
onMultiPointTaskStateChanged
onOverlayError
```

### 10.2 快照字段

```text
MultiPointOverlaySnapshot
- modeEnabled
- taskRunState
- executedActionCount
- currentRound
- currentTargetId
- targets
- overlayUiSettings
- errorCode
```

说明：

- `executedActionCount` 表示本轮任务已完成动作数。
- `currentRound` 表示当前执行到第几轮。
- `currentTargetId` 用于页面展示当前执行点位。
- `targets` 需要包含 Android 校正后的最新位置。

## 11. Flutter 页面建议

### 11.1 首页

首页多点入口从“开发中”调整为可进入，但可根据实现阶段标记：

- 设计中。
- 基础多点可用。
- 高级连招后续支持。

### 11.2 多点模式页面

第一版建议页面结构：

```text
多点模式
- 状态面板
- 点位列表
- 新增点位
- 删除点位
- 调整顺序
- 设置入口
- 开启/关闭多点模式
- 执行/暂停/继续/结束任务
```

设计约束：

- 点位编辑在 App 页面完成。
- 悬浮层只负责展示点位和执行控制。
- 运行中禁用新增、删除、重排，避免执行序列突变。

### 11.3 多点设置页面

设置区：

- 点击间隔。
- 循环次数。
- 无限循环。
- 点击持续时间。
- 悬浮交互模式。

不放全局悬浮外观配置。全局外观继续由首页“全局设置”维护。

### 11.4 高级连招入口

高级连招建议后续独立页面：

```text
高级连招
- 动作列表
- 添加点击
- 添加长按
- 添加滑动
- 添加延迟
- 动作参数编辑
- 方案保存
```

入口可以放在多点模式页面中，但第一版先展示“后续支持”即可。

## 12. Android 架构建议

### 12.1 调度器拆分

建议不要让 `SinglePointClickScheduler` 继续膨胀成所有模式调度器。

后续可以演进为：

```text
AutomationTaskScheduler
- start(actions, repeatPolicy)
- pause()
- resume()
- end()
- currentState()
```

单点模式：

```text
actions = [tap(singleTarget)]
```

基础多点模式：

```text
actions = targets.map { tap(it) }
```

高级连招模式：

```text
actions = userEditedActions
```

### 12.2 坐标提供者

动作执行前需要读取最新坐标，建议使用坐标提供者：

```text
TargetPositionProvider
- positionOf(targetId)
```

价值：

- 点位运行中拖动后，下一次动作使用最新坐标。
- `AutomationAction` 不直接保存过期坐标。
- 单点、多点、连招都可以共用。

### 12.3 手势执行器

建议抽出低层执行器：

```text
AccessibilityGestureExecutor
- tap(x, y, durationMs)
- longPress(x, y, durationMs)
- swipe(fromX, fromY, toX, toY, durationMs)
```

职责：

- 统一处理 `dispatchGesture()` 返回 `false`。
- 统一处理手势完成、取消、服务断开。
- 向调度器返回成功或失败。

这样调度器只关心动作序列，不直接拼 `GestureDescription`。

## 13. 异常与边界

- 无障碍未授权或服务未连接时，禁止开始任务。
- 悬浮窗权限缺失时，禁止开启多点悬浮层。
- 启用点位数量为 0 时，禁止开始任务。
- 点位被拖到屏幕外时，需要通过 `OverlayWindowHelper` 校正到可见范围。
- 屏幕旋转后，所有点位和控制组件都需要重新校正。
- `dispatchGesture()` 返回 `false` 时，任务必须结束并回到 `idle`。
- 手势取消时，任务必须结束或进入安全停止状态，不能卡在 `running`。
- 删除点位时，如果任务正在运行，应先暂停或结束。
- 无限循环时，必须保证 App 页面和悬浮层都有明确结束入口。
- 多点和单点不应同时运行。开启一个模式前需要关闭或拒绝另一个模式。

## 14. 验收标准

基础多点第一版验收：

- 用户可以进入多点模式页面。
- 用户可以新增多个点位。
- 点位显示序号。
- 点位可以拖动并持久化位置。
- 用户可以删除点位并保持序号正确。
- 用户可以调整点位执行顺序。
- 权限完整时可以开启多点悬浮层。
- 悬浮层显示所有启用点位和当前交互模式的控制组件。
- 普通、简洁、极简三种交互模式行为与单点一致。
- 控制条或独立执行控件可以执行、暂停、继续、结束任务。
- 任务按启用点位顺序点击。
- 暂停后继续时不重头执行。
- 结束后再次执行从第一个启用点位开始。
- 运行中拖动点位后，后续点击使用新位置。
- 修改全局悬浮外观后，多点悬浮组件立即刷新且任务不中断。
- 横竖屏切换后所有点位和控制组件保持可见。
- 权限撤销、服务断开、手势派发失败时任务安全收尾。

高级连招后续验收：

- 可创建 tap / longPress / swipe / delay 动作。
- 可调整动作顺序。
- 可编辑每个动作参数。
- 可执行、暂停、继续、结束动作序列。
- 滑动动作使用两个点位或起止坐标。
- 长按和滑动失败时能安全收尾。

## 15. 推荐开发阶段

### 15.1 第一阶段：文档与模型确认

- 完成本文档。
- 确认基础多点与高级连招边界。
- 确认第一版只做基础多点点击。

### 15.2 第二阶段：Flutter 多点模型与持久化

- 新增 `MultiPointTarget`。
- 新增 `MultiPointSettings`。
- 新增多点持久化 store。
- 保存和读取 `multi_point.targets_json`。
- 单元测试覆盖默认值、增删点、排序和持久化。

### 15.3 第三阶段：Flutter 多点页面

- 首页开放多点入口。
- 新增多点模式页面。
- 新增多点设置页面。
- 支持点位列表编辑。
- 暂时用 MethodChannel mock 或兼容空实现承接 Android 未完成能力。

### 15.4 第四阶段：Android 多点 Overlay

- 新增 `MultiPointTargetOverlayComponent`。
- 新增 `MultiPointOverlayManager`。
- 复用控制条、收起图标、独立执行控件。
- 接入全局悬浮外观 metrics。
- 支持多点位置回传和保存。

### 15.5 第五阶段：通用动作调度内核

- 抽出 `AutomationAction`。
- 抽出 `AccessibilityGestureExecutor`。
- 抽出或新增 `AutomationTaskScheduler`。
- 基础多点先只生成 tap 动作。
- 保持单点模式现有行为不回退。

### 15.6 第六阶段：多点联调与异常收尾

- 接入 MethodChannel。
- 完成执行、暂停、继续、结束。
- 处理权限撤销、服务断开、手势失败、横竖屏校正。
- 完成真机验收清单。

### 15.7 第七阶段：高级连招设计与实现

- 基于真实多点使用反馈补充高级连招需求。
- 设计动作列表 UI。
- 开放 longPress / swipe / delay。
- 增加方案保存和导入导出。

## 16. 当前决策

- 下一阶段建议先做基础多点点击，不直接做复杂连招。
- 长按、滑动、延迟应在底层动作模型中预留，但不进入基础多点第一版 UI。
- 高级连招应作为独立模式或高级入口，避免干扰普通多点用户。
- 多点模式必须复用单点已稳定的悬浮交互、任务三态、全局外观和异常收尾策略。
- 单点与多点同一时间只允许一个模式处于开启状态。

## 17. 参考文档

- 单点模式需求文档：`docs/需求文档/单点模式需求文档.md`
- 悬浮交互模式设计文档：`docs/需求文档/悬浮交互模式设计文档.md`
- 全局悬浮外观配置设计文档：`docs/需求文档/全局悬浮外观配置设计文档.md`
- 单点模式真机验收清单：`docs/验收清单/单点模式真机验收清单.md`
- 悬浮窗横竖屏贴边问题：`docs/调试问题/悬浮窗横竖屏贴边问题.md`
